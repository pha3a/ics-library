package pha.ics;

/**
 * Enum encoding the types of the event field values.
 *
 * Created by paul on 08/10/14.
 */
public enum ValueType {
    DATE, INTEGER, TEXT, RECUR, DATE_LIST, TEXT_LIST, STATUS_TYPE, CLASS_TYPE,
    TRANSP_TYPE, DURATION_TYPE, ADDRESS_TYPE;
}
